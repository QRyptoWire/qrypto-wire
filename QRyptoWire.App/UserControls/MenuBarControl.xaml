<UserControl
    x:Class="QRyptoWire.App.UserControls.MenuBarControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:vm ="using:QRyptoWire.Core.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
	d:DataContext="{d:DesignInstance d:Type=vm:MenuViewModel}"
    d:DesignHeight="100">

    <Grid Background="Red">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="*"></RowDefinition>
			<RowDefinition Height="Auto"></RowDefinition>
		</Grid.RowDefinitions>
        <Button Command="{Binding HomeCommand}">
			<TextBlock FontSize="10" Text="Home"></TextBlock>
		</Button>
        <Button Grid.Row="0" Grid.Column="1" Command="{Binding AddContactCommand}">
            <TextBlock FontSize="10" Text="Contacts"></TextBlock>
        </Button>
        <Button Grid.Row="0" Grid.Column="2" Command="{Binding SettingsCommand}">
            <TextBlock FontSize="10" Text="Settings"></TextBlock>
        </Button>
		<Rectangle Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3"
				   Height="8" Fill="Black"></Rectangle>
    </Grid>
</UserControl>
